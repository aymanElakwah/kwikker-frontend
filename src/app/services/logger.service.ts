import { Injectable } from '@angular/core';
/**
 * for showing user freindly pop ups
 */
@Injectable( )
export class LoggerService {
  /**
   * constructor doesn't enject any services for now
   */
  constructor() { }
}
